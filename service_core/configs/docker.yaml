env: "local"
storage_path: "./data/storage.db"
token_ttl: 1h
http_server:
  address: "0.0.0.0:8080"
  timeout: 4s
  idle_timeout: 60s

grpc_address: "constructflow_file:50051"

database:
  host: "postgres"
  port: 5432
  name: "constructflow"
  ssl_mode: "disable"

